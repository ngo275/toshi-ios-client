machine:
  xcode:
    version: "9.1"

test:
  test:
  override:
    - xcodebuild test -workspace Toshi.xcworkspace -scheme "Tests" -destination 'platform=iOS Simulator,name=iPhone 7' | xcpretty --color
  post:
    - bundle exec danger

